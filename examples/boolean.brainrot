skibidi main {
    cap nocap = yes;

    edging(nocap) {
        yapping("It's valid!");
    }
    
    nocap = yes && no;
    yapping("nocap: ", nocap);
    nocap = yes || no;
    yapping("nocap: ", nocap);
}